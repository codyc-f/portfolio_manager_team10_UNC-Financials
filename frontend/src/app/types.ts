import type { Position } from "../types";

export type Page = "holdings" | "performance" | "allocation";
export type Theme = "light" | "dark";
export type TransactionHistoryTarget = Position | "all";
